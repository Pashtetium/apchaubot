{
  "name": "apshubot",
  "version": "1.0.0",
  "description": "Telegram bot for measuring apshu size",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --project tsconfig.json && cp ./Procfile ./dist",
    "start": "npm run build && node --env-file=.env dist/index.js"
  },
  "author": "pashtetium",
  "license": "ISC",
  "engines": {
    "node": "v20.9.0",
    "npm": "10.1.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "mongodb": "^5.1.0",
    "telegraf": "^4.7.0",
    "typegram": "^5.0.0",
    "typescript": "^5.2.2"
  }
}